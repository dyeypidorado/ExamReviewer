<h3>Question:</h3>
<p>
  <%= @question.question %>
</p>
  
<br/>

<h3>Choices</h3>
<table>
<% @choices.each do |choice| %>
  <tr>
    <td><%= choice.choice %> | </td>
    <td><%= link_to 'Edit', edit_admin_exam_question_choice_path(@exam.id, @question.id, choice.id) %> | </td>
    <td><%= link_to 'Delete', admin_exam_question_choice_path(@exam.id, @question.id, choice.id), :method => :delete %></td>
  </tr>
<% end %>
</table>

<br/><hr>

<%= link_to 'Add Choice', new_admin_exam_question_choice_path(@exam.id, @question.id) %> | 
<%= link_to 'Back to Questions', admin_exam_questions_path(@exam.id) %> | 
<%= link_to 'Back to Exams', admin_exams_path %> | 
<%= link_to 'Back to Home', admin_root_path %>  

