.fields
  = f.label :question, "Question"
  = f.text_area :question, :rows => 1, :value => "Question"
  = link_to_remove_fields "remove", f

  = f.fields_for :choices do |builder|
    =render 'choice_fields' , :f => builder
  = link_to_add_fields("Add Choice", f, :choices)
